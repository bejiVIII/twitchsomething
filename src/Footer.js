const Footer = ({ color }) => {
  var style = {
    backgroundColor: color,
  };
  return (
    <footer style={style}>
      <div>made with 💖 by a degenerate</div>
    </footer>
  );
};

export default Footer;
